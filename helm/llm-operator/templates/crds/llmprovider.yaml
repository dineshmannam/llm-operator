# Helm-managed CRD install for LLMProvider.
#
# NOTE: Helm has known limitations around CRD lifecycle (no upgrade, no delete
# by default). For production, consider:
#   Option A: Keep CRDs in config/crd/ and apply them separately in CI before helm upgrade.
#   Option B: Use the crds/ special directory in Helm (auto-applied before other templates).
#
# This file is a symlink-equivalent stub — the real content is in config/crd/llmprovider.yaml.
# In the CI pipeline, a `helm pre-upgrade` step will kubectl-apply the CRDs first.
#
# TODO Weekend 3: decide CRD lifecycle strategy; either embed full YAML here or add CI step.

# Placeholder — see config/crd/llmprovider.yaml for the actual CRD manifest.
